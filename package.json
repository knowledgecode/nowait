{
  "name": "@knowledgecode/nowait",
  "version": "0.2.0",
  "description": "A better async function than the setTimeout(..., 0) for the browser",
  "main": "nowait.js",
  "scripts": {
    "minify": "terser -c -m --ecma 5 -o dist/nowait.min.js --comments some nowait.js && terser -c -m --module -o dist/nowait.es.min.js --comments some nowait.es.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/knowledgecode/nowait.git"
  },
  "keywords": [
    "setImmediate",
    "setTimeout",
    "async"
  ],
  "author": "KNOWLEDGECODE",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/knowledgecode/nowait/issues"
  },
  "homepage": "https://github.com/knowledgecode/nowait",
  "devDependencies": {
    "terser": "^4.3.9"
  }
}
