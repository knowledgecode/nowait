{
  "name": "nowait",
  "version": "0.1.0",
  "description": "The minimal setImmediate() for browser",
  "main": "src/index.js",
  "scripts": {
    "build": "babel src -o lib/nowait.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "KNOWLEDGECODE",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.5.5",
    "@babel/core": "^7.5.5",
    "@babel/preset-env": "^7.5.5"
  }
}
